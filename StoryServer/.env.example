# Local use only. Set secrets in Render Environment for deployment.
OPENAI_API_KEY=replace_with_your_key
OPENAI_MODEL=gpt-4.1-mini
PORT=8787
MAX_GENERATIONS_PER_DAY=200
# Production uses NODE_ENV=production, exact ALLOWED_ORIGINS and DATA_DIR.
